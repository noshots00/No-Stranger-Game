import type { ModifierMap } from '../quests/types';

/** Dev stipend: +10 gold, +10 silver, +10 copper (folded into wallet copper). */
export const DEV_COIN_STIPEND_DELTA: ModifierMap = { gold: 10, silver: 10, copper: 10 };
