/** Addressable replaceable — guild definition per founder (`d` = slug). */
export const NSG_GUILD_DEF_KIND = 30334;

/** Addressable replaceable — one membership per author per guild (`d` = member-{slug}). */
export const NSG_GUILD_MEMBER_KIND = 30335;

export const GUILD_COMMUNITY_TAG = 'guild';
export const GUILD_DEF_TAG = 'guild-def';
export const GUILD_MEMBER_TAG = 'guild-member';

export const GUILD_QUERY_LIMIT_DEFS = 40;
export const GUILD_QUERY_LIMIT_MEMBERS = 80;

/** Poll while Guild Alley dialog is open (ms). */
export const GUILD_FEED_STALE_MS = 20_000;

export const GUILD_MEMBERSHIP_D_PREFIX = 'member-';
